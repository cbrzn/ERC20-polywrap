mutation approve(
  $address: String!,
  $spender: String!,
  $amount: BigInt!
) {
  approve(
    connection: {
      networkNameOrChainId: $network
    }
    address: $address
    spender: $spender
    amount: $amount
  )
}
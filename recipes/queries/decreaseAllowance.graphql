mutation decreaseAllowance(
  $address: String!,
  $spender: String!,
  $subtractedValue: BigInt!
) {
  decreaseAllowance(
    connection: {
      networkNameOrChainId: $network
    }
    address: $address
    spender: $spender
    subtractedValue: $subtractedValue
  )
}